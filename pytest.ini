[pytest]

testpaths =
    tests

python_files =
    test_*.py

addopts =
    -v
    --strict-markers

markers =
    api: API tests
    contract: API contract tests that compare implementation with documentation
    security: Authentication and authorization boundary tests
    ui: UI tests
    smoke: Smoke tests
